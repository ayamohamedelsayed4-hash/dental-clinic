# Dental Clinic Project — Structure Only

This package contains the project structure only.
There is no implementation code.

## Team Responsibilities

- Project Leader: PHP + integration + coordination
- Alia: PHP + admin-side logic
- Enas: Frontend + UI
- Frontend/JS teammate: JavaScript + responsive behavior + validation
- Mohamed: Database design + schema + relationships

## Important

The public pages are `.php` files because the project uses PHP.
Their page interface/HTML will be built inside these files later.
Shared interface parts belong in `includes/`.

Start implementation only after the team agrees on the database structure and page design.
